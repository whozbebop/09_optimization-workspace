# 번들 최적화 실습 프로젝트

이 프로젝트는 Next.js 번들 최적화 기법을 학습하기 위한 실습 자료입니다.  
**의도적으로 최적화되지 않은 코드**를 포함하고 있으며, 학생들이 직접 문제점을 발견하고 개선하는 과정을 통해 번들 최적화를 학습할 수 있습니다.

## 🎯 학습 목표

- 번들 크기가 웹 애플리케이션 성능에 미치는 영향 이해
- 트리쉐이킹(Tree Shaking)과 코드 스플리팅(Code Splitting) 개념 학습
- Next.js에서 동적 import를 활용한 번들 최적화 실습
- 빌드 결과 분석을 통한 성능 측정 방법 습득

---

## 📁 프로젝트 구조

```
02_bundle-optimization/
├── src/
│   └── app/
│       ├── page.tsx                 # 메인 페이지 (네비게이션)
│       ├── editor/
│       │   └── page.tsx             # 코드 에디터 페이지
│       ├── dashboard/
│       │   ├── page.tsx             # 대시보드 메인 페이지
│       │   └── components/
│       │       └── Charts.tsx       # 차트 컴포넌트
│       ├── utils/
│       │   └── page.tsx             # 유틸리티 함수 데모 페이지
│       └── data/
│           └── page.tsx             # 대용량 데이터 페이지
├── package.json                      # 프로젝트 의존성
└── next.config.ts                   # Next.js 설정
```

### 페이지별 설명

#### 1. **홈 페이지 (`/`)**
- 프로젝트 소개 및 각 페이지로 이동하는 네비게이션 역할
- 최적화 실습의 목적과 주요 문제점 안내

#### 2. **에디터 페이지 (`/editor`)**
- Monaco Editor (VS Code와 동일한 에디터)를 포함
- lodash, moment.js 라이브러리 사용
- 코드 스플리팅이 필요한 무거운 컴포넌트 예시

#### 3. **대시보드 페이지 (`/dashboard`)**
- Chart.js와 react-chartjs-2를 사용한 데이터 시각화
- 라인, 바, 파이 차트를 동적으로 전환
- 무거운 시각화 라이브러리의 최적화 필요성 학습

#### 4. **유틸리티 페이지 (`/utils`)**
- lodash의 다양한 유틸리티 함수 사용 (debounce, chunk, shuffle, sum, mean, camelCase 등)
- moment.js를 사용한 날짜 처리
- axios를 사용한 API 호출
- 트리쉐이킹이 필요한 라이브러리 import 패턴 예시

#### 5. **데이터 페이지 (`/data`)**
- 1000개의 상품 데이터를 인라인으로 정의
- lodash를 사용한 복잡한 데이터 처리 (필터링, 정렬, 통계 계산)
- 대용량 정적 데이터의 최적화 방법 학습

---

## 📦 사용 라이브러리

### 1. **@monaco-editor/react** (약 3MB+)
```json
"@monaco-editor/react": "^4.7.0"
```
- **설명**: VS Code에서 사용하는 Monaco Editor의 React 래퍼
- **용도**: 브라우저에서 코드 편집 기능 제공
- **문제점**: 매우 무거운 라이브러리로 직접 import 시 초기 번들 크기 급증
- **사용 위치**: `/editor` 페이지

### 2. **lodash** (약 70KB)
```json
"lodash": "^4.17.21"
```
- **설명**: JavaScript 유틸리티 라이브러리 (배열, 객체, 문자열 처리 등)
- **용도**: debounce, chunk, shuffle, sum, mean, camelCase 등 다양한 헬퍼 함수 제공
- **문제점**: 전체 import 시 트리쉐이킹이 되지 않아 사용하지 않는 함수도 번들에 포함
- **사용 위치**: `/editor`, `/dashboard`, `/utils`, `/data` 페이지

### 3. **moment** (약 300KB)
```json
"moment": "^2.30.1"
```
- **설명**: 날짜/시간 처리 라이브러리 (현재는 레거시로 간주됨)
- **용도**: 날짜 포맷팅, 계산, 조작
- **문제점**: 
  - 무거운 라이브러리 (모든 로케일 데이터 포함)
  - 트리쉐이킹 불가능 (CommonJS 방식)
  - 현재는 유지보수 모드 (새로운 기능 개발 중단)
- **대안**: date-fns, day.js 등 경량 라이브러리 권장
- **사용 위치**: `/editor`, `/utils` 페이지, `/dashboard` 컴포넌트

### 4. **chart.js** (약 150KB)
```json
"chart.js": "^4.5.1"
```
- **설명**: JavaScript 차트 라이브러리
- **용도**: 라인, 바, 파이 등 다양한 차트 시각화
- **문제점**: 직접 import 시 코드 스플리팅이 되지 않음
- **사용 위치**: `/dashboard` 페이지

### 5. **react-chartjs-2** (약 10KB)
```json
"react-chartjs-2": "^5.3.1"
```
- **설명**: Chart.js의 React 래퍼 컴포넌트
- **용도**: Chart.js를 React에서 쉽게 사용하기 위한 컴포넌트 제공
- **사용 위치**: `/dashboard` 페이지

### 6. **axios** (약 13KB)
```json
"axios": "^1.13.2"
```
- **설명**: HTTP 클라이언트 라이브러리
- **용도**: API 요청 및 응답 처리
- **문제점**: 간단한 GET/POST 요청은 브라우저 내장 fetch API로 충분한 경우가 많음
- **사용 위치**: `/utils` 페이지

### 7. **Next.js** (코어 프레임워크)
```json
"next": "15.5.7",
"react": "19.1.0",
"react-dom": "19.1.0"
```
- **설명**: React 기반 풀스택 프레임워크
- **특징**: 
  - 자동 코드 스플리팅
  - 서버 사이드 렌더링 (SSR)
  - 정적 사이트 생성 (SSG)
  - API Routes
  - Turbopack (빠른 번들러)

---

## ⚠️ 현재 프로젝트의 문제점

### 1. **트리쉐이킹이 되지 않는 import 패턴**

#### ❌ 나쁜 예 (현재 코드)
```javascript
import _ from "lodash";           // lodash 전체 import (70KB)
import moment from "moment";      // moment 전체 import (300KB)

// 실제로는 몇 개 함수만 사용
_.debounce(fn, 300);
_.chunk(array, 3);
moment().format("YYYY-MM-DD");
```

#### ✅ 좋은 예 (최적화 후)
```javascript
// 개별 함수만 import (트리쉐이킹 가능)
import debounce from "lodash/debounce";     // 약 2KB
import chunk from "lodash/chunk";           // 약 2KB

// 또는 경량 라이브러리 사용
import { format } from "date-fns";          // 필요한 함수만 (약 10KB)
```

**영향**: 사용하지 않는 코드도 번들에 포함되어 번들 크기가 불필요하게 커짐

---

### 2. **코드 스플리팅이 되지 않는 무거운 컴포넌트**

#### ❌ 나쁜 예 (현재 코드)
```javascript
// 직접 import → 페이지를 방문하지 않아도 번들에 포함됨
import Editor from "@monaco-editor/react";  // 3MB+
import { Line, Bar, Pie } from "react-chartjs-2";  // Chart.js 150KB
```

#### ✅ 좋은 예 (최적화 후)
```javascript
// 동적 import → 페이지를 방문할 때만 로드
import dynamic from "next/dynamic";

const Editor = dynamic(() => import("@monaco-editor/react"), {
  ssr: false,
  loading: () => <p>에디터 로딩 중...</p>
});
```

**영향**: 
- 모든 페이지의 초기 로딩 속도가 느려짐
- 사용하지 않는 페이지의 코드도 다운로드됨
- First Load JS (초기 로딩 JavaScript) 크기 증가

---

### 3. **대용량 데이터가 인라인으로 포함됨**

#### ❌ 나쁜 예 (현재 코드)
```javascript
// 1000개의 데이터를 코드 내에 직접 정의
const products = Array.from({ length: 1000 }, (_, i) => ({
  id: i + 1,
  name: `상품 ${i + 1}`,
  // ... 많은 필드들
}));
```

#### ✅ 좋은 예 (최적화 후)
```javascript
// 별도 JSON 파일로 분리하거나 API로 가져오기
import products from "./data/products.json";
// 또는
const products = await fetch("/api/products").then(res => res.json());
```

**영향**: 
- 정적 데이터가 JavaScript 번들에 포함되어 크기 증가
- 데이터가 변경되지 않는데도 매번 다운로드됨

---

### 4. **레거시 라이브러리 사용**

#### ❌ moment.js 사용 (현재)
```javascript
import moment from "moment";
moment().format("YYYY-MM-DD");  // 300KB
```

#### ✅ date-fns 또는 day.js 사용 (권장)
```javascript
import { format } from "date-fns";
format(new Date(), "yyyy-MM-dd");  // 약 10KB (필요한 함수만)

// 또는
import dayjs from "dayjs";
dayjs().format("YYYY-MM-DD");  // 약 7KB
```

**영향**: 
- 불필요하게 무거운 라이브러리로 인한 성능 저하
- 유지보수가 중단된 레거시 코드 사용

---

## 📊 빌드 결과 해석 (npm run build)

### 빌드 명령어 실행
```bash
npm run build
```

### 빌드 결과 예시
```
Route (app)                         Size  First Load JS
┌ ○ /                                0 B         117 kB
├ ○ /_not-found                      0 B         114 kB
├ ○ /dashboard                   66.1 kB         287 kB  ⚠️
├ ○ /data                        6.29 kB         145 kB
├ ○ /editor                      9.24 kB         226 kB  ⚠️
└ ○ /utils                       22.8 kB         243 kB  ⚠️
+ First Load JS shared by all     120 kB         ⚠️
  ├ chunks/8082ab48faca5ea1.js   17.2 kB
  ├ chunks/a75771d769c2c47e.js   24.1 kB
  ├ chunks/b5b02ffdec2cb59a.js   59.2 kB
  └ other shared chunks (total)  19.3 kB

○  (Static)  prerendered as static content
```

---

### 📈 결과 해석 방법

#### 1. **Size (페이지별 크기)**
각 라우트(페이지)에 **고유하게 포함된** JavaScript 코드의 크기입니다.

```
├ ○ /dashboard                   66.1 kB  ← 대시보드에만 있는 코드
├ ○ /data                        6.29 kB  ← 데이터 페이지에만 있는 코드
├ ○ /editor                      9.24 kB  ← 에디터에만 있는 코드
└ ○ /utils                       22.8 kB  ← 유틸리티에만 있는 코드
```

**분석 포인트**:
- 큰 값 = 해당 페이지에 많은 고유 코드가 포함됨
- `/dashboard`가 66.1KB로 가장 큼 → Chart.js 관련 코드가 포함되어 있음

---

#### 2. **First Load JS (초기 로딩 JavaScript)**
해당 페이지를 **처음 방문할 때 다운로드해야 하는 총 JavaScript 크기**입니다.

```
First Load JS = Size + First Load JS shared by all
```

예시:
```
/dashboard: 66.1 kB (페이지 고유) + 120 kB (공유) = 186.1 kB
```

하지만 실제로는 **287 kB**로 표시됨!

**왜 차이가 날까요?**
- 66.1 kB: `/dashboard` 페이지의 고유 코드
- 120 kB: 모든 페이지가 공유하는 기본 코드 (React, Next.js 런타임 등)
- **나머지 약 100 kB**: `/dashboard`에서 사용하는 외부 라이브러리 (Chart.js, lodash 등)

**분석 포인트**:
- `/dashboard`: **287 kB** ⚠️ 가장 무거움 (Chart.js 때문)
- `/editor`: **226 kB** ⚠️ 두 번째로 무거움 (Monaco Editor 때문)
- `/utils`: **243 kB** ⚠️ lodash, moment, axios 포함
- `/data`: **145 kB** 상대적으로 가벼움
- `/`: **117 kB** 메인 페이지는 가벼움

---

#### 3. **First Load JS shared by all (모든 페이지가 공유하는 코드)**
```
+ First Load JS shared by all     120 kB
  ├ chunks/8082ab48faca5ea1.js   17.2 kB
  ├ chunks/a75771d769c2c47e.js   24.1 kB
  ├ chunks/b5b02ffdec2cb59a.js   59.2 kB  ⚠️ 가장 큰 청크
  └ other shared chunks (total)  19.3 kB
```

**포함 내용**:
- React 및 React-DOM 코어 라이브러리
- Next.js 런타임 (라우팅, 페이지 로딩 등)
- 모든 페이지에서 공통으로 사용하는 코드

**분석 포인트**:
- 이 값은 **한 번만 다운로드**되고, 이후 페이지 이동 시 재사용됨
- 120 kB는 Next.js + React 애플리케이션의 기본 오버헤드
- `b5b02ffdec2cb59a.js` (59.2 kB)가 가장 큰 청크 → React 코어로 추정

---

### 🎯 성능 지표 및 권장 기준

#### **Google의 권장 기준**
- **First Load JS < 100 KB**: 우수 ✅
- **First Load JS 100-200 KB**: 보통 ⚠️
- **First Load JS > 200 KB**: 개선 필요 ❌

#### **현재 프로젝트 상태**
| 페이지 | First Load JS | 평가 | 문제 라이브러리 |
|--------|---------------|------|-----------------|
| `/` | 117 kB | ⚠️ 보통 | - |
| `/dashboard` | **287 kB** | ❌ 나쁨 | Chart.js, lodash |
| `/editor` | **226 kB** | ❌ 나쁨 | Monaco Editor, moment, lodash |
| `/utils` | **243 kB** | ❌ 나쁨 | lodash, moment, axios |
| `/data` | 145 kB | ⚠️ 보통 | lodash |

---

### 🔍 문제 진단 및 개선 방향

#### 1. `/dashboard` (287 kB) - 가장 무거움
**원인**:
- Chart.js (150KB) + react-chartjs-2가 직접 import됨
- lodash 전체 import

**개선 방법**:
```javascript
// 1. Chart 컴포넌트를 dynamic import로 변경
const Charts = dynamic(() => import("./components/Charts"), {
  ssr: false,
  loading: () => <div>차트 로딩 중...</div>
});

// 2. lodash를 개별 함수로 import
import range from "lodash/range";
```

**예상 개선**: 287 kB → 약 130 kB (157 kB 감소)

---

#### 2. `/editor` (226 kB)
**원인**:
- Monaco Editor가 직접 import됨
- moment.js (300KB)
- lodash 전체 import

**개선 방법**:
```javascript
// 1. Monaco Editor를 dynamic import
const Editor = dynamic(() => import("@monaco-editor/react"), {
  ssr: false,
  loading: () => <p>에디터 로딩 중...</p>
});

// 2. moment → date-fns 또는 day.js로 교체
import { format } from "date-fns";

// 3. lodash → 개별 import
import debounce from "lodash/debounce";
```

**예상 개선**: 226 kB → 약 125 kB (101 kB 감소)

---

#### 3. `/utils` (243 kB)
**원인**:
- lodash 전체 (70KB)
- moment 전체 (300KB)
- axios (13KB)

**개선 방법**:
```javascript
// 1. lodash → 개별 import
import debounce from "lodash/debounce";
import chunk from "lodash/chunk";
// 또는 네이티브 JavaScript 사용

// 2. moment → date-fns로 교체
import { format, addDays } from "date-fns";

// 3. axios → fetch API 사용
const response = await fetch(url);
const data = await response.json();
```

**예상 개선**: 243 kB → 약 125 kB (118 kB 감소)

---

#### 4. `/data` (145 kB)
**원인**:
- 1000개 아이템의 대용량 데이터가 인라인으로 정의됨
- lodash 전체 import

**개선 방법**:
```javascript
// 1. 데이터를 별도 JSON 파일로 분리
import products from "./data/products.json";

// 2. lodash → 개별 import 또는 네이티브 사용
// filter, orderBy, chunk 등을 네이티브 JavaScript로 구현
```

**예상 개선**: 145 kB → 약 120 kB (25 kB 감소)

---

### 📌 최적화 우선순위

#### **우선순위 1 (High Impact)**: 무거운 라이브러리 코드 스플리팅
- Monaco Editor → dynamic import
- Chart.js → dynamic import
- **예상 효과**: 초기 로딩 속도 2-3배 개선

#### **우선순위 2 (Medium Impact)**: 트리쉐이킹 개선
- lodash 전체 import → 개별 함수 import
- moment → date-fns 또는 day.js로 교체
- **예상 효과**: 번들 크기 300-400KB 감소

#### **우선순위 3 (Low Impact)**: 데이터 최적화
- 대용량 데이터 별도 파일 분리
- axios → fetch API 사용
- **예상 효과**: 번들 크기 30-50KB 감소

---
